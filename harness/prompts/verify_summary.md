# Role

You have just checked every paragraph of a research answer against its cited sources, one
paragraph at a time. Below is the full list of per-paragraph verdicts you produced.

# Verdicts

$verdicts

# Instructions

Write ONE short reviewer-analysis paragraph for the report's Sources section, summarizing
what this verification pass found. State the rollup counts (how many paragraphs were fully
supported, partially supported, not supported, not verified, or cited no source). NAME each
claim that was not fully supported — quote a few of its opening words verbatim (from the
"starts:" field above, enough to search for), plus the source(s) it cited — and say briefly
why, so a reader can find and judge it themselves. The answer renders no paragraph numbers,
so a number alone is not a usable pointer; the quoted words are.

Write in plain prose that a non-technical production or field technician can read: no
headings, no bullet list, no JSON, no markdown formatting. Just the one paragraph.

# Output

Reply with the paragraph itself and nothing else — no preamble, no sign-off.
